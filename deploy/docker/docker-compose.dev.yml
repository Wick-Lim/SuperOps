# Development overrides - use with:
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
#
# Only starts infrastructure services (DB, Redis, NATS, MinIO, Meilisearch)
# Run backend locally + RN app via Expo.

services:
  backend:
    profiles: ["disabled"]
  worker:
    profiles: ["disabled"]
  migrate:
    profiles: ["disabled"]
